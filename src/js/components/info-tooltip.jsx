import React from 'react';

let InfoTooltip = React.createClass({
    render() {
        return (
            <div className="field-row__error js-error-tooltip"></div>
        );
    }
});

export default InfoTooltip;
